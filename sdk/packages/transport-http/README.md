# @servify/transport-http

Reserved package for future HTTP and long-polling transport implementations.

Current exports only define shape contracts:

- `HttpRequestTransportConfig`
- `LongPollingTransportConfig`
