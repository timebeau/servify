name = "servify-website"
main = "src/worker.ts"
compatibility_date = "2025-01-01"

# Serve static assets from the website source (apps/website)
[assets]
directory = "../website"
binding = "ASSETS"

# Custom domains (configured in Cloudflare dashboard)
# These will be automatically picked up by wrangler deploy
# custom_domains = ["www.servify.cloud", "servify.cloud"]
